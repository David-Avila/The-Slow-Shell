# Custom shell for MiniMicro
This is a shell that adds more flexibility to the default shell.

# How to install
Download `shell.ms` and `startup.ms` and put them aside the MiniMicro executable. If you use a custom path for `/usr`, then copy this files to the root of that folder.

**Documentation will be added soon**
